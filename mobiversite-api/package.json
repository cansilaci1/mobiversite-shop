{
  "name": "mobiversite-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo \"no build step for json-server\"",
    "start": "json-server --host 0.0.0.0 --port $PORT --static . db.json"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
